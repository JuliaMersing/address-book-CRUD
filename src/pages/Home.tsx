import React from 'react';
import { Header } from '../components/Header/Header';

export const Home: React.FunctionComponent = () => (
	<Header heading="Contacts" href="/" linkParagraph="Go back to address book" />
);
